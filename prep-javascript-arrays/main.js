//First Problem for Practicing Array

const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

console.log('My array of the variable languages: ', languages);

//Time to code problem 2
let firstElement;
let thirdElement;

firstElement = languages[0];
thirdElement = languages[2];

console.log('My first index in my array is:', firstElement);
console.log('My third index in my array is:', thirdElement);

//Time to code question 3

let length;

length = languages.length;

console.log('The length of the array is:', length);

//Time to code question 4
let lastElement;

const index = languages.length - 1;

lastElement = languages[index];

console.log('The last element of the array is:', lastElement);
